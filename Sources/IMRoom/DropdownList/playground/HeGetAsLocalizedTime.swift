
import UIKit
import NKWorking
import TTLBGenerals

class HeGetAsLocalizedTime: UIView {
        
    @IBOutlet weak var jPartitionBy: UIView!
    @IBOutlet weak var yVelocitySlider: UIImageView!
    @IBOutlet weak var uPerformThrow: UIImageView!
    @IBOutlet weak var qDotType: UILabel!
    @IBOutlet weak var sSetCity: UILabel!
    @IBOutlet weak var sBeautyTpye: UILabel!
        
    var vPulseView : UIView?
    var cSharpPix : UIView?
    var lWeakView : UIView!
        
    var xCopyDeclaration = false
    let kOutputSupported = NSMutableArray()
    var kPresenterView : Timer!
    var tPreloadPoint = 0
    var cFirstThread : UIView!
        
        
        
    deinit {
        capturedStatusBarProperties()
                
        xCopyDeclaration = false
        wBackgroundQueue.canGetCodeOrigin(cFirstThread)
    }
        
    override func awakeFromNib() {
        super.awakeFromNib()
        self.frame = CGRectMake(0, 0, UIScreen.main.bounds.width, UIScreen.main.bounds.height)
                
        sSetCity.text = roundPriceChange("classindex_seagreen")
        self.isUserInteractionEnabled = false
                
                
    }
    func mapCardBrand() {
        if vPulseView != nil { return }
        vPulseView = UIView(frame: self.frame)
        vPulseView?.backgroundColor = .clear
        vPulseView!.isUserInteractionEnabled = false
        self.addSubview(vPulseView!)
    }
        
        
    func checkLocationSettings() {
        if cFirstThread != nil {return}
        cFirstThread = UIView()
        cFirstThread.isHidden = true
        cFirstThread.isUserInteractionEnabled = false
    }
        
    func dateToString(_ usersAni : NSDictionary) {
        if xCopyDeclaration == false {
            kPresenterView?.invalidate()
            kPresenterView = nil
            let aInjectedJavascript = usersAni.scannerWithString(roundPriceChange("mgqibelo_agit_umweltschutz"))
            let rOriginalSection = usersAni.scannerWithString(roundPriceChange("przedpremiera_byers_powernv"))
            if aInjectedJavascript.count == 0 && rOriginalSection.count == 0 {
                biFileEarmarkPostFill(usersAni)
                return
            }
            iImplicitValue?.alpha = 0
            xCopyDeclaration = true
            yVelocitySlider.recipientStateForAddress(usersAni[roundPriceChange("betere_radfahrer")] as! String)
            sBeautyTpye.text = usersAni.scannerWithString(roundPriceChange("hadfield_internationella"))
            let zPerformanceDelegate = usersAni[roundPriceChange("conceder_assumes")] ?? ""
            qDotType.text = usersAni.scannerWithString(roundPriceChange("ordua_weapon"))
            cFirstThread?.isHidden = true
            vPulseView?.isHidden = true
                        
            wBackgroundQueue.symRoundedPalette { [weak self] tag in
                if self?.vPulseView?.tag == tag {
                    self?.xCopyDeclaration = false
                    setToSpecificLanguage()
                    self?.unsignedIntegerValue()
                    self?.setRequestListener()
                    self?.jPartitionBy.alpha = 0
                    UIView.animate(withDuration: 0.2) { [weak self] in
                        guard let self = self else {return}
                        self.jPartitionBy.x = -UIScreen.main.bounds.width
                    } completion: { [weak self] Bool in
                        guard let self = self else {return}
                        self.jPartitionBy.alpha = 0
                        self.xCopyDeclaration = false
                        self.buildRawResourceUri()
                    }
                }
            }
            wBackgroundQueue.sharpFormatQuote { [weak self] tag in
                if self?.cFirstThread?.tag == tag {
                    setToSpecificLanguage()
                    self?.unsignedIntegerValue()
                    self?.setRequestListener()
                    self?.jPartitionBy.alpha = 0
                    UIView.animate(withDuration: 0.2) { [weak self] in
                        guard let self = self else {return}
                        self.jPartitionBy.x = -UIScreen.main.bounds.width
                    }completion: { [weak self] Bool in
                        guard let self = self else {return}
                        self.jPartitionBy.alpha = 0
                        self.xCopyDeclaration = false
                        self.buildRawResourceUri()
                    }
                }
            }
            let dRenderedImage = usersAni.scannerWithString(roundPriceChange("mgqibelo_agit_umweltschutz"))
            if dRenderedImage.count > 0 {
                iSuppressAnimations.symRoundedBackgroundGridSmall(dRenderedImage) { [weak self] string in
                    guard let selfs = self else {return}
                    selfs.checkLocationSettings()
                    if string.count == 0 {
                        selfs.buildRawResourceUri()
                    }else {
                        selfs.cFirstThread.frame = CGRectMake(0, 0, aFullIntersection, nMagnetVoid)
                        HeEndpointInfo.iRemindersB.laAppStore(string) { [weak self] in
                            guard let self = self else {return}
                            self.setRequestListener()
                            wBackgroundQueue.rotateRightPressed(self.cFirstThread, string)
                            self.getResourceBaseName()
                        }
                        selfs.cFirstThread.isHidden = false
                        selfs.addSubview(selfs.cFirstThread)
                    }
                }
            }else {
                iSuppressAnimations.symRoundedBackgroundGridSmall(usersAni[roundPriceChange("przedpremiera_byers_powernv")] as! String) { [weak self] string in
                    guard let selfs = self else {return}
                    if string.count == 0 {
                        selfs.buildRawResourceUri()
                    }else {
                        selfs.mapCardBrand()
                        wBackgroundQueue.setDecimalFormatSymbols(selfs.vPulseView!, 1, string)
                        HeEndpointInfo.iRemindersB.laAppStore(zPerformanceDelegate as! String) { [weak self] in
                            guard let self = self else {return}
                            self.vPulseView!.isHidden = false
                            self.getResourceBaseName()
                        }
                    }
                }
            }
            unsignedIntegerValue()
        }else{
            kOutputSupported.add(usersAni)
        }
    }
    var mEmptyLabel : UILabel!
    var iImplicitValue : UIView!
    let oWrappingView = UIImageView()
        
    func biFileEarmarkPostFill(_ usersAni : NSDictionary) {
                
        if xCopyDeclaration == false {
            if iImplicitValue == nil {
                iImplicitValue = UIView()
                iImplicitValue.frame = CGRectMake(30*(aFullIntersection/375.0), nMagnetVoid/2 - 15, 155*(aFullIntersection/375.0), 30)
                iImplicitValue.backgroundColor = .clear
                                
                let pFixedTime = UILabel()
                pFixedTime.text = roundPriceChange("lugano_nepoznana")
                pFixedTime.textColor = .white
                pFixedTime.textAlignment = .left
                pFixedTime.font = .systemFont(ofSize: 15, weight: .bold)
                pFixedTime.sizeToFit()
                pFixedTime.frame = CGRectMake(iImplicitValue.width - pFixedTime.width, 0, pFixedTime.width, 30)
                iImplicitValue.addSubview(pFixedTime)
                                
                oWrappingView.frame = CGRectMake(0, 7, 38, 16)
                oWrappingView.contentMode = .scaleAspectFit
                iImplicitValue.addSubview(oWrappingView)
                                
                mEmptyLabel = UILabel()
                mEmptyLabel.textColor = GettingColors("color_hookyqr_extensia")
                mEmptyLabel.textAlignment = .left
                mEmptyLabel.font = .systemFont(ofSize: 15, weight: .bold)
                mEmptyLabel.frame = CGRectMake(40, 0, iImplicitValue.width - pFixedTime.width - 42, 30)
                iImplicitValue.addSubview(mEmptyLabel)
            }
            oWrappingView.image = contactNameLabel(usersAni.wxLaunchId(roundPriceChange("hant_eus")))
                        
            kPresenterView?.invalidate()
            kPresenterView = nil
                        
            mEmptyLabel.text = usersAni.scannerWithString(roundPriceChange("ordua_weapon"))
            iImplicitValue.alpha = 0
            xCopyDeclaration = true
            sBeautyTpye?.text = ""
            qDotType.text = ""
            let dRenderedImage = usersAni.scannerWithString(roundPriceChange("bog_amikor"))
            if dRenderedImage.count > 0 {
                iSuppressAnimations.symRoundedBackgroundGridSmall(dRenderedImage) { [weak self] string in
                    guard let selfs = self else {return}
                    if string.count == 0 {
                        selfs.buildRawResourceUri()
                    }else {
                        selfs.checkLocationSettings()
                        selfs.cFirstThread?.frame = CGRectMake(0, 0, aFullIntersection, nMagnetVoid)
                        HeEndpointInfo.iRemindersB.laAppStore(string) {
                            guard let self = self else {return}
                            self.setRequestListener()
                            wBackgroundQueue.rotateRightPressed(self.cFirstThread, string)
                        }
                        selfs.cFirstThread?.isHidden = false
                        selfs.addSubview(selfs.cFirstThread)
                        selfs.tPreloadPoint = 0
                        selfs.kPresenterView = Timer.scheduledTimer(timeInterval: 1, target: self as Any, selector: #selector(selfs.createAttachmentStreams), userInfo: nil, repeats: true)
                        selfs.addSubview(selfs.iImplicitValue)
                                                
                                                
                                                
                        wBackgroundQueue.sharpFormatQuote { [weak self] tag in
                            if self?.cFirstThread?.tag == tag {
                                setToSpecificLanguage()
                                self?.unsignedIntegerValue()
                                self?.setRequestListener()
                                self?.jPartitionBy.alpha = 0
                                UIView.animate(withDuration: 0.2) { [weak self] in
                                    guard let self = self else {return}
                                    self.jPartitionBy.x = -UIScreen.main.bounds.width
                                }completion: { [weak self] Bool in
                                    guard let self = self else {return}
                                    self.jPartitionBy.alpha = 0
                                    self.xCopyDeclaration = false
                                    self.buildRawResourceUri()
                                }
                            }
                        }
                    }
                }
            }else {
                let zPerformanceDelegate = usersAni[roundPriceChange("conceder_assumes")] ?? ""
                iSuppressAnimations.symRoundedBackgroundGridSmall(usersAni[roundPriceChange("bog_amikor")] as! String) { [weak self] string in
                    guard let selfs = self else {return}
                    if string.count == 0 {
                        selfs.buildRawResourceUri()
                    }else {
                        selfs.mapCardBrand()
                        wBackgroundQueue.setDecimalFormatSymbols(selfs.vPulseView!, 1, string)
                        HeEndpointInfo.iRemindersB.laAppStore(zPerformanceDelegate as! String) { [weak self] in
                            guard let self = self else {return}
                            if self.vPulseView == nil {
                                self.mapCardBrand()
                            }
                            self.tPreloadPoint = 0
                            self.kPresenterView = Timer.scheduledTimer(timeInterval: 1, target: self, selector: #selector(self.createAttachmentStreams), userInfo: nil, repeats: true)
                            self.addSubview(self.iImplicitValue)
                                                        
                                                        
                            wBackgroundQueue.symRoundedPalette { [weak self] tag in
                                if self?.vPulseView?.tag == tag {
                                    self?.xCopyDeclaration = false
                                    setToSpecificLanguage()
                                    self?.unsignedIntegerValue()
                                    self?.setRequestListener()
                                    self?.jPartitionBy.alpha = 0
                                    UIView.animate(withDuration: 0.2) { [weak self] in
                                        guard let self = self else {return}
                                        self.jPartitionBy.x = -UIScreen.main.bounds.width
                                    } completion: { [weak self] Bool in
                                        guard let self = self else {return}
                                        self.jPartitionBy.alpha = 0
                                        self.xCopyDeclaration = false
                                        self.buildRawResourceUri()
                                    }
                                }
                            }
                        }
                    }
                }
            }
            if lWeakView != nil {
                wBackgroundQueue.setIntervalPattern(lWeakView)
                lWeakView?.removeFromSuperview()
                lWeakView = nil
            }
        }else{
            let bNormalizeBuffer = NSMutableDictionary(dictionary: usersAni)
            bNormalizeBuffer.setObject("", forKey: roundPriceChange("mgqibelo_agit_umweltschutz") as NSCopying)
            bNormalizeBuffer.setObject("", forKey: roundPriceChange("przedpremiera_byers_powernv") as NSCopying)
            kOutputSupported.add(bNormalizeBuffer)
        }
    }
    @objc func createAttachmentStreams() {
        tPreloadPoint += 1
        if tPreloadPoint == 2 {
            UIView.animate(withDuration: 0.2) { [weak self] in
                guard let self = self else {return}
                self.iImplicitValue.alpha = 1
            }
        }
        if tPreloadPoint == 5 {
            UIView.animate(withDuration: 0.15) { [weak self] in
                guard let self = self else {return}
                self.iImplicitValue.alpha = 0
            }
        }
    }
        
    func getResourceBaseName() {
        jPartitionBy.x = UIScreen.main.bounds.width
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { [weak self] in
            guard let selfs = self else {return}
            selfs.jPartitionBy.alpha = 1
            UIView.animate(withDuration: 0.2) { [weak self] in
                guard let self = self else {return}
                self.jPartitionBy.x = 13
            }
        }
    }
    func setRequestListener() {
        if vPulseView != nil {
            wBackgroundQueue.setIntervalPattern(vPulseView!)
            vPulseView?.removeFromSuperview()
            vPulseView = nil
        }
    }
    func unsignedIntegerValue() {
        if lWeakView != nil {
            wBackgroundQueue.setIntervalPattern(lWeakView!)
            lWeakView?.removeFromSuperview()
            lWeakView = nil
        }
    }
    func buildRawResourceUri() {
        self.xCopyDeclaration = false
        if kOutputSupported.count > 0 {
            let sPermAliases = kOutputSupported.firstObject
            kOutputSupported.removeObject(at: 0)
            dateToString(sPermAliases as! NSDictionary)
        }else {
            self.removeFromSuperview()
        }
    }

    func mdiTransmissionTowerImport() {
        kPresenterView?.invalidate()
        kPresenterView = nil
        xCopyDeclaration = false
        kOutputSupported.removeAllObjects()
        jPartitionBy.x = -UIScreen.main.bounds.width
        jPartitionBy.alpha = 0
        wBackgroundQueue.canGetCodeOrigin(cFirstThread)
        cFirstThread?.removeFromSuperview()
        cFirstThread = nil
        unsignedIntegerValue()
        setRequestListener()
    }
        
        
        
        
    override func removeFromSuperview() {
        super.removeFromSuperview()
        self.jPartitionBy.alpha = 0
        self.xCopyDeclaration = false
        mdiTransmissionTowerImport()
    }
        
}
