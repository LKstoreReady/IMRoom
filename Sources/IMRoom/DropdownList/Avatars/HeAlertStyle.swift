
import UIKit
import TTLBGenerals
import NKWorking

class HeAlertStyle: NSObject {
        
        
    var gInsertUpdate = 0
    var qRoundPageview = ""
    var mToolbarView = ""
    var gPressedData = ""
    var bEighthStream = false
    var tDocumentReference : HeIconPreview!
    var jStartDir : [HeCreateToneCurve] = []
    var wFullscreenUpdate = 0
    var yNumPoints = 0.0
        
    var nSharpSlideshow = 0
    var eTrackActivity = ""
    var fReceivedHangup = 0.0
    var bPresentedController = 120.0
        
    @MainActor init(_ dic : NSDictionary) {
        super.init()
        gInsertUpdate = dic.wxLaunchId(oBootstrapVersion)
        qRoundPageview = dic.scannerWithString(eCatchRollback)
        gPressedData = dic.scannerWithString(lRecordingFrames)
        bEighthStream = dic.setTimeInSeconds(pGetImage)
        wFullscreenUpdate = dic.wxLaunchId(nPrepareDelete)
                
        tDocumentReference = HeIconPreview(dic.setLoadingBackgroundColor(bNumStars))
                
        let gStartJob = dic.getEncodedAuthority(gKeywordName)
        jStartDir = HeCreateToneCurve.performBlockAndWait(gStartJob)
                
        var bReferringParams : UILabel! = UILabel()
        bReferringParams.width = aFullIntersection - 24
        bReferringParams.font = .systemFont(ofSize: 14, weight: .medium)
        bReferringParams.numberOfLines = 0
        bReferringParams.text = gPressedData
        bReferringParams.sizeToFit()
        yNumPoints = bReferringParams.height
        bReferringParams.text = ""
        bReferringParams = nil
                
    }
        
    @MainActor func biSquareHalf() {
        nSharpSlideshow = 1
        let yUpdateModel = Locale.preferredLanguages.first
        let bStrictEqual = yUpdateModel?.components(separatedBy: "-")
        let vTokenizeString = NSMutableDictionary()
        vTokenizeString.setObject(qRoundPageview, forKey: hTranslateMap as NSCopying)
        vTokenizeString.setObject((bStrictEqual?.first)!, forKey: dMallocString as NSCopying)
        iSuppressAnimations.readBooleanUsingCompat(vTokenizeString) { [weak self] dics in
            if dics.allKeys.count > 0 {
                self?.mToolbarView = dics.scannerWithString(hTranslateMap)
            }
            self?.deleteSomeExpiredStories()
        } _: { [weak self] in
            self?.nSharpSlideshow = 0
            UIView.fadeToBlack("videa_titles_slight", "")
        }
    }
        
    @MainActor func deleteSomeExpiredStories() {
        let yUpdateModel = Locale.preferredLanguages.first
        let bStrictEqual = yUpdateModel?.components(separatedBy: "-")
        let vTokenizeString = NSMutableDictionary()
        vTokenizeString.setObject(gPressedData, forKey: hTranslateMap as NSCopying)
        vTokenizeString.setObject((bStrictEqual?.first)!, forKey: dMallocString as NSCopying)
        iSuppressAnimations.readBooleanUsingCompat(vTokenizeString) { [weak self] dics in
            if dics.allKeys.count > 0 {
                self?.nSharpSlideshow = 2
                self?.eTrackActivity = dics.scannerWithString(hTranslateMap)
                                
                var bReferringParams : UILabel! = UILabel()
                bReferringParams.width = aFullIntersection - 24
                bReferringParams.font = .systemFont(ofSize: 14, weight: .medium)
                bReferringParams.numberOfLines = 0
                bReferringParams.text = dics.scannerWithString(hTranslateMap)
                bReferringParams.sizeToFit()
                self?.fReceivedHangup = bReferringParams.height
                bReferringParams.text = ""
                bReferringParams = nil
            }
            UIView.fadeToBlack("videa_titles_slight", "")
        } _: { [weak self] in
            self?.nSharpSlideshow = 0
            UIView.fadeToBlack("videa_titles_slight", "")
        }
    }
        
        
    @MainActor class func performBlockAndWait(_ array : NSArray) -> [HeAlertStyle] {
        let kOutputSupported = NSMutableArray()
        for vTokenizeString in array {
            let oBundledPath = HeAlertStyle(vTokenizeString as! NSDictionary)
            kOutputSupported.add(oBundledPath)
        }
        return kOutputSupported as! [HeAlertStyle]
    }
        
        
}


class HeCreateToneCurve : NSObject {
        
    var gInsertUpdate = 0
    var qClockListener = 0
    var iGetFacets = ""
    var iGetFloat = ""
    var wDrawPercent = ""
    var tHighInside = ""
    var hContainerTo = 0
        
    init(_ dic : NSDictionary) {
        super.init()
        gInsertUpdate = dic.wxLaunchId(oBootstrapVersion)
        qClockListener = dic.wxLaunchId(fRequestAd)
        iGetFacets = dic.scannerWithString(eRecipientIdentities)
        iGetFloat = dic.scannerWithString(pSingleRow)
        wDrawPercent = dic.scannerWithString(kOnUnlock)
        tHighInside = dic.scannerWithString(sMinUnit)
        hContainerTo = dic.wxLaunchId(cStrExtensions)
    }
        
        
        
    class func performBlockAndWait(_ array : NSArray) -> [HeCreateToneCurve] {
        let uGestureMode = NSMutableArray()
        for dics in array {
            let oBundledPath = HeCreateToneCurve(dics as! NSDictionary)
            uGestureMode.add(oBundledPath)
        }
        return uGestureMode as! [HeCreateToneCurve]
    }
}
